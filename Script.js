// Soma dos valores do vetor.
let soma = 0
let numeros = [1, 8, 9, 10]

for (let i = 0; i < numeros.length; i++) {
    // let valor = numeros[i]
    //soma += valor
    soma = soma + numeros[i]
}

console.log(soma)

function maioMenorNumero() {

    let soma = 0
    let menor = 1000
    let maior = -1
    let numeros[5, 6, 7, 8, 9];
    for (let i = 0; i < numeros.length; i++) {
        soma = soma + numeros[i]
        if(maior < numeros[i]){
            maior = numeros[i]
        }
        if(menor > numeros[i]){
            menor = numeros[i]
        }
      
        } 
    }

console.log(soma)
console.log(maior)
console.log(menor)
